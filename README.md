# Cold Case Files (Demo)

This repository contains a Vite + React + TypeScript demo of the Cold Case Files engine and a minimal UI.

Run locally:

1. npm install
2. npm run dev

## Running Tests

To run the tests, first install the required dependencies:

```bash
npm install
```

Then, run the tests with:

```bash
npm run dev
```
